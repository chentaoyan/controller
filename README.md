# controller
ARGoS

# Set up

``` $ cd controller ```

``` $ cd cockroach ```

``` $ cd build ```

``` $ cmake .. ```

``` $ make ```

``` $ cd .. ```

``` $ argos3 -c experiments/lua_project.argos ```

